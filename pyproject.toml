[tool.ruff]
line-length = 100
target-version = "py311"
exclude = [
    "venv",
    "alembic",
]

[tool.ruff.lint]
select = ["E", "F", "I"]
ignore = []

[tool.ruff.lint.isort]
known-first-party = ["api", "core", "db", "models", "services", "schemas", "worker"]

